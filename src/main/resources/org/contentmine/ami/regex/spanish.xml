<compoundRegex title="spanish">
  <regex  fields="cielo">cielo</regex>
  <regex  fields="madrid">Madrid</regex>
  <regex  fields="brillo">brillo</regex>
  <regex  fields="contaminacio´n">contaminacio´n</regex>
  
</compoundRegex>